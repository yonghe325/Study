1. 选中点,一分为二,分为左右区间(函数头)
2. 在左右区间继续 步骤一(递归步骤,递归出口)
+ 此时只需要关心第一次步骤一,然后递归传参
```c++
void merge(int left , int right)//函数头
{
	int mid =(left+right)/2;
	merge(legt,mid);
	//归并左边
	merge(mid+1,right);
	//归并右边
	//合并有序区间
	...
}
```