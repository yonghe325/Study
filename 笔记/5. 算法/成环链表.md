[环形链表](https://leetcode.cn/problems/linked-list-cycle-ii/solutions/441131/huan-xing-lian-biao-ii-by-leetcode-solution/)

# 判断一条链表是否成环
```C
struct ListNode *detectCycle(struct ListNode *head) {
    if(!head || !head->next || !head->next->next  ){
        return NULL;
    }
    struct ListNode *slow = head->next;
    struct ListNode *fast = head->next->next;
    while(slow != fast){
        if(!fast->next || !fast->next->next){
            return NULL;
        }
        slow=slow->next;
        fast = fast->next->next;
    }
    //两指针相遇
    fast = head;
    while(fast != slow){
        fast = fast->next;
        slow=slow->next;
    }
    return fast;
}
```
# 判断多条链表是否相交成环
共有以下情况
+ 两条链表不相交,不成环
+ 两条链表不相交,但成环
	![[Pasted image 20251210162229.png]]
	思路 :==判断两个结点是否相同==
+ 两条链表相交后成环
	![[Pasted image 20251210162406.png]]
	思路 :==判断两个结点是否相同==
+ 两条链表相交于环上
	![[Pasted image 20251210162448.png]]
	思路 :固定一个链表的成环结点,让另一个结点在环上迭代
	如果能相交,则成立

