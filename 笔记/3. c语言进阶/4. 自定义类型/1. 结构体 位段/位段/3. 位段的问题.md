# int类型
int位段被当成有符号数还是无符号数是不确定的
应确定int类型是
unsigned int 类型
还是
signed int类型
# 最大数目不确定
16位系统位段最大数为16bit位
32位系统位段最大数为32bit位
当位段被定义为20时,在16位系统会产生异常
# 内存中的顺序
位段中的成员在内存中
==一个字节里==
是==从左向右==还是==从右向左==分配标准还未确定
# bit位是否剩余还是利用
当一个结构体有两个位段时
第二个位段较大,第一个字节的空间无法开辟第二个位段时
该字节的剩余bit位是被利用还是浪费是不确定的