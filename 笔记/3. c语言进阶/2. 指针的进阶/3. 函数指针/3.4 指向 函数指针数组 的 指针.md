指针指向一个数组
数组的每个元素都是函数指针
![[函数指针套娃.png]]
```
int(*pfArr[5])(int, int) = { 0,Add,Sub,Mul,Div };
//函数指针数组

int(*(*ppfArr)[5])(int, int) = &pfArr;
//指向 函数指针数组 的 指针
```
1. \*先与ppfArr结合成为指针
2. \(\*ppfArr)指向
有\[5]个元素的数组
每个元素类型为 int(\*)(int,int) 类型
