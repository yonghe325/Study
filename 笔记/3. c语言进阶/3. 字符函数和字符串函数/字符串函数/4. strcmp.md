```
int strcmp( const char *lhs, const char *rhs );
```
以==字典序==比较二个空终止字节字符串
分别比较ASCLL码值
结果的符号是被比较的字符串中首对不同字符（都转译成 unsigned char ）的值间的差的符号。
+ 返回值 : 
 1. 若字典序中 `lhs` 先出现于 `rhs` 则为负值
 2. 若 `lhs` 与 `rhs` 比较相等则为零
 3. 若字典序中 `lhs` 后出现于 `rhs` 则为正值