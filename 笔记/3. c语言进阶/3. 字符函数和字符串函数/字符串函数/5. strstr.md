```
char *strstr( const char* str, const char* substr );
```
查找 `substr` 所指的空终止字节字符串在 `str` 所指的空终止字节字符串中的首次出现
==查找子串==
+ 不比较空字符串
+ 若 `str` 或 `substr` 不是指向空终止字节字符串的指针，则行为未定义
+ 返回值 : 
 1. 指向于 `str` 中找到的子串首字符的指针，或若找不到该子串则为空指针NULL
 2.  若 `substr` 指向空字符串，则返回 `str` 
