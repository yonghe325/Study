```
char *strcat( char *dest, const char *src );
```
追加 `src` 所指向的空终止字节字符串的==副本==到 `dest` 所指向的空终止字节字符串的结尾。
+ 从dest指向数组的第一个'\0'开始追加
+ 若目标数组对于 `src` 和 `dest` 的内容以及空终止符不够大，则行为未定义
+ 若字符串==重叠==,==内存溢出==,行为未定义
+ 若 `dest` 或 `src` 不是指向空终止字节字符串的指针，则行为未定义
+ `dest`指向的空间必须可修改

返回dest的副本

==类似于strcpy函数==