```
char* strerror( int errnum );
```
打印 错误码(errnum) 对应的错误信息
# errno
c语言设置的一个全局的错误码存放的变量
只要函数调用错误
则该函数会将对应的错误码存入errno
# 使用
```
printf("%s\n",strerror(0));
printf("%s\n",strerror(1));
printf("%s\n",strerror(2));
printf("%s\n",strerror(3));
printf("%s\n",strerror(4));
...
```
