```
void* memcpy( void *dest, const void *src, size_t count );
```
从 `src` 所指向的对象复制 `count` 个==字节==到 `dest` 所指向的对象
两个对象都被转译成 unsigned char 的数组
+ 若访问发生在 dest 数组结尾后则行为未定义
+ 若对象重叠 (这违背 restrict 契约) , 则行为未定义
memcpy只适合两个独立的内存拷贝
+ `dest` 或 `src` 为非法或空指针则行为未定义