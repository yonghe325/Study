# 串联成环
+ 当有相交节点时
![[Pasted image 20251130181425.png]]
A : a1->a2->c1->c2->c3->b1->b2->b3->c1
B : b1->b2->b3->c1->c2->c3->a1->a2->c1
将A,B成环遍历,保证AB环的节点个数相同
使得AB相交之前的节点互补
使得AB最终遍历到同一处
```
struct ListNode *getIntersectionNode(struct ListNode *headA, struct ListNode *headB) {
    if(!headA || !headB){
        return NULL;
    }
    struct ListNode *pA = headA;
    struct ListNode *pB = headB;
    while(pA != pB){
        pA= pA ? pA->next : headB;
        pB =pB ? pB->next : headA;
    }
    return pA;
}
```
# 先走k步
分别求出AB的长度
让长的先走两链表长度的差值
保证最终走到同一个节点