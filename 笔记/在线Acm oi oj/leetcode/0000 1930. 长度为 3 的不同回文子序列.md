```cpp
class Solution {
public:
    int countPalindromicSubsequence(string s) {
        set<char>arr;
        map<char,int>mid;
        for(int i =0; i<s.size();i++){
            if(arr.count(s[i])){
                mid[s[i]] = i;
                //存放可以组成回文串的字符最后出现的下标
                // cout<<s[i]<<" "<<mid[s[i]]<<endl;
            }
            else arr.insert(s[i]);
        }
        set<char>().swap(arr);
        //清空arr
        int ret = 0;
        for(int i =0;i<s.size();i++){
            if(mid.count(s[i])){
                for(int j = i+1; j < mid[s[i]];j++){
                    arr.insert(s[j]);
                    // cout<<j<<" "<<s[j]<<endl;
                }
                mid.erase(s[i]);
                ret+=arr.size();
            }
            set<char>().swap(arr);
            // if(arr.size()&&arr.count(s[i+1])) arr.erase(s[i+1]);
            //重复利用arr
        }
        return ret;
    }
};
```
# 灵神
## 枚举两侧
对26个字母进行遍历
查找符合左右两侧各有一个/多个的字母
在对中间的字母进行去重遍历
最后中间的字母个数就是回文子串的个数
```cpp
class Solution {
public:
    int countPalindromicSubsequence(string s) {
        int ans = 0;
        for (char alpha = 'a'; alpha <= 'z'; alpha++) { // 枚举两侧字母 alpha
            int i = s.find(alpha); // 最左边的 alpha 的下标
            if (i == string::npos) { // s 中没有 alpha
                continue;
            }
            int j = s.rfind(alpha); // 最右边的 alpha 的下标
  
            bool has[26]{};
            for (int k = i + 1; k < j; k++) { // 枚举中间字母 s[k]
                if (!has[s[k] - 'a']) {
                    has[s[k] - 'a'] = true; // 避免重复统计
                    ans++;
                }
            }
        }
        return ans;
    }
};
```
## 枚举中间+前后缀分解
```cpp
class Solution {
public:
    int countPalindromicSubsequence(string s) {
        int n = s.size();
        // 统计 [1,n-1] 每个字母的个数
        int suf_cnt[26]{};
        for (int i = 1; i < n; i++) {
            suf_cnt[s[i] - 'a']++;
        }
        
        bool pre_has[26]{};
        bool has[26][26]{};
        int ans = 0;
        for (int i = 1; i < n - 1; i++) { // 枚举中间字母 mid
            int mid = s[i] - 'a';
            suf_cnt[mid]--; // 撤销 mid 的计数，suf_cnt 剩下的就是后缀 [i+1,n-1] 每个字母的个数
            pre_has[s[i - 1] - 'a'] = true; // 记录前缀 [0,i-1] 有哪些字母
            for (int alpha = 0; alpha < 26; alpha++) { // 枚举两侧字母 alpha
                // 判断 mid 的左右两侧是否都有字母 alpha
                if (pre_has[alpha] && suf_cnt[alpha] && !has[mid][alpha]) {
                    has[mid][alpha] = true;
                    ans++;
                }
            }
        }
        return ans;
    }
};
```