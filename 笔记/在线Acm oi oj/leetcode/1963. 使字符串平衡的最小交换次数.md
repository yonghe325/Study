## self
```cpp
class Solution {
public:
    int minSwaps(string s) {
        stack<char>arr;
        for(int i = 0; i<s.size();i++){
            if(!arr.empty() && arr.top() =='[' && s[i] == ']'){
                arr.pop();
            }
            else{
                arr.push(s[i]);
            }
        }
        //只存放不平衡的字符串
        //每两个字符只有三种可能
        //[[ , ]], ][
        vector<int>ret(3,0);
        int ans = 0;
        while(!arr.empty()){
            if(arr.top() == '['){
                arr.pop();
                if(arr.top() == '['){
                    ret[0]++;
                }
                else{
                    ret[2]++;
                }
            }
            else{
                arr.pop();
                ret[1]++;
            }
            arr.pop();
        }
        cout<<ret[0]<<" "<<ret[1]<<" "<<ret[2]<<endl;
        //ret[0], ret[1]每一对添加一次次数
        //不成对的时候每一个算一个次数
        //ret[2]一个一次
        ans = min(ret[0],ret[1]);
        ret[0]-=ans;
        ret[1]-=ans;
        for(int i = 0; i<ret.size();i++){
            ans+=ret[i];
        }
        return ans;
    }
};
```
## 灵神
最左边的`]`总是和最右边的`[`的交换 
```cpp
class Solution {
public:
    int minSwaps(string s) {
        int ans = 0, c = 0;
        //c记录当前不成对 [ 的数目
        int j = s.size() - 1;
        for (char b : s) {
            if (b == '[') {
                c++;
                //当为[时, c++
                //即将[的个数+1
            } else if (c > 0) {
                c--;
                //c>0 且 b == ']'
                //说明该]与前面的一个'['成对
                //将c--
            } else { // c == 0
            //当b == ']' 且前面没有'['
                // 找最右边的左括号交换
                while (s[j] == ']') {
                    j--;
                }
                s[j] = ']'; // s[i] = '[' 可以省略
                ans++;
                c++; // s[i] 变成左括号，c 加一
            }
        }
        return ans;
    }
};
```
### 优化1 去掉ans
在前文的例子中，`[][[[[` 的后两个括号本应是右括号
本应把 c 执行两次 −1，最终得到 c=0]]但我们反而执行了两次 +1，最终得到 c=4
换句话说，每次交换都会让最终的 c 增大 2（把 −1 改成了 +1）
所以最终的 c 除以 2，便是交换次数。
```cpp
class Solution {
public:
    int minSwaps(string s) {
        int c = 0;
        for (char b : s) {
            if (b == '[' || c == 0) {
                c++;
            } else {
                c--;
            }
        }
        return c / 2;
    }
};
```
## 优化2
```cpp

```