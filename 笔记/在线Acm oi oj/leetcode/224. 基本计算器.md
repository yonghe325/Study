## slef
我的逻辑 : 
+ 通过`()`的出现判断嵌套的深度
+ 前缀运算符的出现: 
	将单个嵌套分为两层, 内嵌套, 外嵌套
	即在一个嵌套(`()`)出现前, 在`num`中`push`一个0
	即在一个嵌套(`()`)内所有数出现前, `push`一个0
		防止`1+ (  -2)`的发生, 等价于`1+ 0 + (0 - 2)`
	最后如果有运算符未运算, 在将所有嵌套合并`1+ 0 + (0 - 2)`
异常 : 
+ 当`(0 - 2)`运算时, 回将内嵌套的标志运算掉, 导致
	内嵌套合并变为`0 + (0 - 2)`, 外嵌套合并变为`1+ 0 + (0 - 2)`
	运算发生混乱
```cpp 失败
class Solution {
public:
    int calculate(string s) {
        stack<char>op;
        stack<int>num;
        num.push(0);
        for(int i = 0;i<s.size();i++){
            if(s[i] >='0' && s[i]<='9'){
                int mid = i;
                while(mid<s.size() && s[mid] >='0' && s[mid]<='9'){
                    mid++;
                }
                num.push(stoi(s.substr(i,mid-i)));
                cout<<"压入数字 : "<<num.top()<<endl;
                i = mid-1;
            }
            else if(s[i] =='-' || s[i] =='+'){
                int mid = i+1;
                while(s[mid] == ' ') mid++;//处理前导空格
                if(s[mid] =='('){
                    op.push(s[i]);
                    //存放未运算的运算符
                    cout<<"遇到(, 压入外嵌套0, 压入运算符 : "<<op.top()<<endl;
                    cout<<"此时未运算的运算符的数量 : "<<op.size()<<endl;
                    continue;
                }
                int start = mid;
                while( mid<s.size() && s[mid] >='0' && s[mid]<='9'){
                    mid++;
                }
                start = stoi(s.substr(start,mid-i));
                cout<<"运算 : "<<num.top()<<" "<<s[i]<<" "<<start;
                if(s[i] =='-') num.top()-=start;
                else num.top()+=start;
                i = mid-1;
                cout<<" = "<<num.top()<<endl;
                cout<<"此时mid = "<<mid<<endl;
                // i = mid-1;
            }
            else if(s[i] =='('){
                num.push(0);
                //先压入外嵌套0
                num.push(0);
                //在压入内嵌套0
                //方式前缀运算符的发生
            }
            else if(s[i] ==')'){
                //需要合并栈顶的两个数
                //一定是0 与 num.top()
                int mid = num.top();
                num.pop();
                cout<<"合并内嵌套 : "<<num.top() <<" ++ "<<mid<<endl;
                num.top()+=mid;
   XXX//如果有前缀运算符的出现,不用合并外嵌套
                mid = num.top();
                num.pop();
                cout<<"合并外嵌套 : "<<num.top() <<" ++ "<<mid<<endl;
                num.top()+=mid;
                //如果有上层嵌套,与上层嵌套的数合并
                if(op.empty()) continue;
                mid = num.top();
                num.pop();
                cout<<"与上一层按照符号嵌套和并"<<num.top()<<" "<<op.top()<<" ";
                if(op.top() == '-') num.top()-=mid;
                else num.top()+=mid;
                cout<<num.top()<<endl;
                op.pop();
            }
            //s[i] ==' '时, 什么也不做
            cout<< i <<endl<<endl;
        }
        //i已经到达终点
        //最外层嵌套还没有合并
        cout<<"循环完毕, 此时未运算的运算符的数量 : "<<op.size()<<endl;
        while(!op.empty()){
            int mid = num.top();
            num.pop();
            cout<<"最后合并: "<<num.top()<<"++"<<mid;
            if(op.top() == '-') num.top()-=mid;
            else num.top()+=mid;
            cout<<num.top()<<endl;
            op.pop();
            cout<<"此时未运算的运算符的数量 : "<<op.size()<<endl;
        }
        return num.top();
    }
};
// 数字栈
//当遇到'(', 切换栈顶, 否则在原来栈顶上运算
// 运算符栈
//当遇到运算符时
// 将栈顶于下一个数字进行运算, 运算完后, 将该运算符出栈
// 如果遇到嵌套, 就跳过此次运算, 压入新的栈顶
```
## 宫水三叶
## 双栈解法
![[Pasted image 20260404120935.png]]
```cpp
class Solution {
public:
    void replace(string& s){
        int pos = s.find(" ");
        while (pos != -1) {
            s.replace(pos, 1, "");
            pos = s.find(" ");
        }
    }
    int calculate(string s) {
        // 存放所有的数字
        stack<int> nums;
        // 为了防止第一个数为负数，先往 nums 加个 0
        nums.push(0);
        // 将所有的空格去掉
        replace(s);
        // 存放所有的操作，包括 +/-
        stack<char> ops;
        int n = s.size();
        for(int i = 0; i < n; i++) {
            char c = s[i];
            if(c == '(')
                ops.push(c);
            else if(c == ')') {
                // 计算到最近一个左括号为止
                while(!ops.empty()) {
                    char op = ops.top();
                    if(op != '(')
                        calc(nums, ops);
                    else {
                        ops.pop();
                        break;
                    }
                }
            }
            else {
                if(isdigit(c)) {
                    int cur_num = 0;
                    int j = i;
                    // 将从 i 位置开始后面的连续数字整体取出，加入 nums
                    while(j <n && isdigit(s[j]))
                        cur_num = cur_num*10 + (s[j++] - '0');
                    // 注意上面的计算一定要有括号，否则有可能会溢出
                    nums.push(cur_num);
                    i = j-1;
                }
                else {
                    if (i > 0 && (s[i - 1] == '(' || s[i - 1] == '+' || s[i - 1] == '-')) {
                        nums.push(0);
                    }
                    // 有一个新操作要入栈时，先把栈内可以算的都算了
                    while(!ops.empty() && ops.top() != '(')
                        calc(nums, ops);
                    ops.push(c);
                }
            }
        }
        while(!ops.empty())
            calc(nums, ops);
        return nums.top();
    }
    void calc(stack<int> &nums, stack<char> &ops) {
        if(nums.size() < 2 || ops.empty())
            return;
        int b = nums.top(); nums.pop();
        int a = nums.top(); nums.pop();
        char op = ops.top(); ops.pop();
        nums.push(op == '+' ? a+b : a-b);
    }
};
```