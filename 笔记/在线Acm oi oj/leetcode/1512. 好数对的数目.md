```cpp
class Solution {
public:
    int numIdenticalPairs(vector<int>& nums) {
        map<int,int>arr;
        int sum = 0;
        for(int i = 0; i< nums.size();i++){
            arr[nums[i]]++;
            sum+=arr[nums[i]]-1;
        }
        return sum;
    }
};
```