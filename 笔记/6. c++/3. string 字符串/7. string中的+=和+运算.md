表示在指定字符串之后加上另一个字符串(可以是一个字符)
本质是`string`中重载了`operatoe+=`这个操作符
+ 尾部拼接
```c++
	string s = "hello";
	cout << s+" world" << endl;
	s += " world";
	cout << s << endl;
```
+ 头部拼接
```c++
	string s = " world";
	cout << "hello"+s << endl;
```