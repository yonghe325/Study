```c
<string>.push_back(char str);
```
在字符串后面插入元素
```c
string s;
s.push_back('h');
s.push_back('e');
s.push_back('l');
s.push_back('l');
s.push_back('o');
cout << s<<endl;
```
+ 当插入多个字符时
	只会添加`str`的最后一个字符
```c
s.push_back('love');
//s = "e";
```
+ 遍历添加
```c
string s;]
char c;
for(c = 'a';c<='z';c++){
	s.push_back(c);
}
cout << s << endl;
```