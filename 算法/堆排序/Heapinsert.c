#include "head.h"
void Heapinsert(int*arr,int length) {
	//新添加的元素始终在数组最后
	//判断是否为堆
	int index = length - 1;
	//插入元素位置
	int top = (index - 1) / 2;
	//父元素
	while (index) {
		if (arr[top]<arr[index])
		{

		}
	}
}